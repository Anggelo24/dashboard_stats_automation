# =============================================================================
# FRONTEND ENVIRONMENT VARIABLES (VITE_*)
# These variables are embedded into the client-side bundle during build
# =============================================================================

# Google Sheets Configuration (unique per client)
VITE_GOOGLE_SHEETS_URL=https://script.google.com/your-google-sheets-url

# Client Branding (unique per client)
VITE_LOGO_URL=https://your-logo-url.com/logo.png
VITE_CLIENT_NAME=Client Name Here
VITE_CLIENT_ID=cliente_example_001

# =============================================================================
# BACKEND ENVIRONMENT VARIABLES (Vercel Serverless Functions)
# These variables are ONLY available server-side in /api functions
# IMPORTANT: Never expose API keys or credentials in VITE_* variables
# =============================================================================

# N8N Backend Configuration
# Get these from your N8N instance settings
N8N_BASE_URL=https://your-n8n-instance.com/api/v1
N8N_API_KEY=your_n8n_api_key_here

# Email Configuration for Support Tickets
# For Google Workspace: use smtp.gmail.com with port 587
# IMPORTANT: Use App Password, not your regular password
# Generate App Password: https://myaccount.google.com/apppasswords
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@yourdomain.com
EMAIL_PASSWORD=your_16_character_app_password_here
